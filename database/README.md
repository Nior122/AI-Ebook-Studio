# Database

This directory holds database planning and migration assets.

- `migrations/`: Alembic migration files when initialized
- `schema/`: schema notes and SQL references
- `seeds/`: local seed data
- `diagrams/`: ER diagrams

No schema is implemented in Stage 1.
